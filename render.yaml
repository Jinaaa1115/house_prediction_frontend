services:
  - type: web
    name: house-price-frontend
    env: static
    staticPublishPath: .
    buildCommand: echo "No build needed"
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
